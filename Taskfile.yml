# https://taskfile.dev

version: '3'

vars:

tasks:
  build-zd:
    dir: cmd/zendesk
    cmds:
      - go build -o main -buildvcs=false

  run-zd:
    dir: cmd/zendesk
    cmds:
      - go run main.go

  run-zd-dev:
    dir: cmd/zendesk
    cmds:
      - air
  
  run-us:
    dir: mockUserService
    cmds:
      - json-server --config json-server.json --routes routes.json --watch db.json

